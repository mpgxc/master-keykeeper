generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Secret {
  id           String @id @default(uuid())
  title        String
  username     String
  password     String
  customFields Json?
}
